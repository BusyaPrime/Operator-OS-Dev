# Operator OS Dev

Phone-first trusted operator system.

This repository now contains the Phase 0-2 monorepo foundation:

- `apps/` for API, mobile, and desktop runtime scaffolds
- `packages/` for shared contracts, config, and tooling
- `docs/` for source-of-truth architecture and bootstrap notes
- `infra/` for Cloud Build, Cloud Run, and supporting scripts

Current branch for substantive work: `bootstrap/foundation-v1`.

TODO(phase-3): expand this README with full architecture, local setup, build flow, and GCP resource mapping.
