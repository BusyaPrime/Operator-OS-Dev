import { createStubModule } from '../module-utils.js';

export const authModule = createStubModule(
  'auth',
  'Firebase / Identity Platform validation is reserved for the next implementation phase.'
);
