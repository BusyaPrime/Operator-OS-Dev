import { createStubModule } from '../module-utils.js';

export const storageModule = createStubModule(
  'storage',
  'Firestore and Cloud Storage integrations are documented but not implemented yet.'
);
