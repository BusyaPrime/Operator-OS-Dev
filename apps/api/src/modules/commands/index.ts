import { createStubModule } from '../module-utils.js';

export const commandsModule = createStubModule(
  'commands',
  'Command intake and Cloud Tasks dispatch scaffolding is present, but handlers are not connected yet.'
);
