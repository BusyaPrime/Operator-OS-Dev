import { createStubModule } from '../module-utils.js';

export const telemetryModule = createStubModule(
  'telemetry',
  'Telemetry event publishing is scaffolded but not connected to Pub/Sub yet.'
);
