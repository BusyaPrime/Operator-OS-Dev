import { createStubModule } from '../module-utils.js';

export const providersModule = createStubModule(
  'providers',
  'Provider abstraction is available; additional provider routing is intentionally out of scope for bootstrap.',
  'ok'
);
