import { createStubModule } from '../module-utils.js';

export const sessionsModule = createStubModule(
  'sessions',
  'Trusted session lifecycle persistence and approval gates are not connected yet.'
);
